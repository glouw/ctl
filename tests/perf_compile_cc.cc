#include <deque>
#include <list>
#include <map>
#include <queue>
#include <set>
#include <stack>
#include <vector>
#include <string>

int main()
{
    std::deque<int> a;
    std::list<int> b;
    std::map<int, int> c;
    std::queue<int> d;
    std::set<int> e;
    std::stack<int> f;
    std::vector<int>g;
    std::string h;
}
